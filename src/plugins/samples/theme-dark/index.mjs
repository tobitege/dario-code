/**
 * Theme Dark plugin — config-only, no runtime behavior
 *
 * Theme values are read from manifest.json config by the TUI layer.
 * This entry point is a no-op.
 */

export function activate() {}

export function deactivate() {}
